# Vercel deployment configuration
# This tells Vercel to use our custom build setup

# Ignore everything except what we need
node_modules/
.git/
.vercel/

# Ensure Vercel uses our build command from vercel.json
# NOT react-scripts build
